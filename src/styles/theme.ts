/**
 * Визуальная система «Второго Канона» для видеоформата.
 *
 * База — шесть цветов бренд-бука (чернильный, молочный, бордо, тёмно-зелёный,
 * латунь, клинический серый). Для физиологического кодирования добавлены
 * производные оттенки: подсвеченные варианты тех же цветов и два
 * функциональных акцента вегетативной регуляции.
 */

export const colors = {
  /** Основа: почти чёрный графит. Фон 80% кадров. */
  ink: '#0F1A1E',
  /** Глубокая тень для виньетки. */
  inkDeep: '#070E11',
  /** Приподнятая панель поверх фона. */
  inkRaised: '#16242A',
  /** Молочный: старая бумага, слоновая кость. Основной текст. */
  milk: '#F3ECDF',
  /** Приглушённый молочный для вторичного текста. */
  milkDim: '#C6BFB1',
  /** Бордо: миокард, повреждение, опасность. */
  oxblood: '#6E2B30',
  /** Освещённый миокард (систола, активная ткань). */
  oxbloodLit: '#9C4149',
  /** Латунь: электрический импульс, точка внимания. */
  brass: '#B08D3F',
  /** Раскалённая латунь: фронт возбуждения. */
  brassLit: '#E7C475',
  /** Тёмно-зелёный: анатомическая норма, структура. */
  pine: '#1D3B34',
  /** Холодный сине-зелёный: парасимпатическое влияние. */
  teal: '#3E8F86',
  /** Подсвеченный сине-зелёный. */
  tealLit: '#6FC0B5',
  /** Тёплый золотисто-красный: симпатическое влияние. */
  sympathetic: '#C87A42',
  /** Клинический серый: сетки, оси, служебные подписи. */
  grey: '#7E868A',
  /** Серый для тонких линий на тёмном. */
  greyDim: '#4A5257',
} as const;

export const fonts = {
  /** Заголовки и основной текст. */
  sans: "'Inter', 'Liberation Sans', 'DejaVu Sans', sans-serif",
  /** Коды записей, клинические данные, подписи к схемам. */
  mono: "'IBM Plex Mono', 'DejaVu Sans Mono', monospace",
  /** Голос Авиценны — единственное типографское «украшение». */
  serif: "'Noto Serif', 'Liberation Serif', 'DejaVu Serif', serif",
} as const;

/** Формат Reels и безопасные зоны Instagram. */
export const layout = {
  width: 1080,
  height: 1920,
  /** Важный текст не ближе 160 px к верхнему и нижнему краю. */
  safeTop: 160,
  safeBottom: 160,
  safeSide: 84,
  /** Полоса выходных данных (@vtoroy.kanon) — сразу под верхней границей. */
  brandY: 178,
  /** Рабочая сцена: анатомия, графики, схемы. */
  stageTop: 250,
  stageBottom: 1400,
  /** Зона субтитров — выше нижней служебной области Reels. */
  subtitleTop: 1440,
  subtitleBottom: 1690,
} as const;

/** Типографская шкала кадра 1080×1920. */
export const type = {
  hero: 104,
  h1: 76,
  h2: 58,
  h3: 44,
  body: 38,
  label: 30,
  micro: 24,
  tiny: 20,
} as const;

export const shadows = {
  /** Мягкое свечение латунью — активный электрический процесс. */
  brassGlow: (strength = 1) =>
    `drop-shadow(0 0 ${10 * strength}px rgba(231,196,117,${0.55 * strength})) drop-shadow(0 0 ${
      30 * strength
    }px rgba(176,141,63,${0.35 * strength}))`,
  tealGlow: (strength = 1) =>
    `drop-shadow(0 0 ${10 * strength}px rgba(111,192,181,${0.5 * strength}))`,
  textDepth: '0 2px 18px rgba(0,0,0,0.55)',
} as const;
