/**
 * Подключение канонического образа Авиценны.
 *
 * ────────────────────────────────────────────────────────────────────────
 *  КАК ПОДКЛЮЧИТЬ ЭТАЛОННОЕ ИЗОБРАЖЕНИЕ
 * ────────────────────────────────────────────────────────────────────────
 *  1. Положите утверждённый файл персонажа в
 *       public/assets/avicenna/avicenna-reference.png
 *     (подойдёт .png с прозрачным фоном, .webp или .jpg;
 *      рекомендуемый размер — не меньше 1200 px по высоте,
 *      кадрирование «портрет-полуоборот, грудь до плеч»).
 *  2. Впишите путь в константу AVICENNA_IMAGE ниже:
 *       export const AVICENNA_IMAGE: string | null = 'assets/avicenna/avicenna-reference.png';
 *  3. Перерендерите ролик: npm run render:reel
 *
 *  Пока константа равна null, компонент AvicennaCharacter рисует
 *  каноническую графическую фигуру наблюдателя, построенную строго по
 *  character bible проекта: чапан прямого кроя, воротник-стойка,
 *  застёжка-Канон у ключицы, наручный измеритель с одной линией пульса,
 *  стилус. Лицо не изображается — вместо портрета используется силуэт
 *  со спины-полуоборота, чтобы не подменять утверждённый образ
 *  случайным «восточным мудрецом».
 *
 *  Изображение вставляется с object-fit: contain и никогда не растягивается.
 */
export const AVICENNA_IMAGE: string | null = null;

/** Код записи архива для этого ролика. */
export const RECORD_CODE = 'К2 · I · М-002';

/** Хэндл проекта — виден на протяжении всего ролика. */
export const HANDLE = '@vtoroy.kanon';
